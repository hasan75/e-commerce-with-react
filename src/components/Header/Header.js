import React from 'react';
import logo from '../../images/logo.png';

const Header = () => {
    return (
        <div>
            <h2>Header</h2>
            <img src={logo} alt="img-logo" srcset="" />
        </div>
    );
};

export default Header;